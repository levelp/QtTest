sudo: required
dist: trusty
language: cpp

install:
  - sudo apt-get install -y qt5-default qttools5-dev-tools #install necessary Qt files

script:
  #- qmake "CONFIG+=test" QtTest.pro #we gonna compile for Unit test first
  #- make
  #- ./QtTest #run unit test
  #- rm Makefile QtTest *.o #remove files (which are generated from unit test) for next build
  - qmake QtTest.pro #we gonna compile for actual program
  - make
  - ls 
  - ./QtTest #run unit test